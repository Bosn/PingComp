<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>PingComp · 编辑</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="brand">
        <img src="/logo.svg" class="logo" alt="PingComp logo">
        <div>
          <h1>编辑客户 #<%= row.id %></h1>
          <p>保存后将自动锁定，后续同步不覆盖。</p>
        </div>
      </div>
      <a class="btn" href="/">返回列表</a>
    </div>

    <div class="card" style="padding:16px;">
      <form method="post" action="/edit/<%= row.id %>">
        <div class="edit-grid">
          <label class="field">
            <span>Name</span>
            <input class="input" name="name" value="<%= row.name || '' %>">
          </label>
          <label class="field">
            <span>Region</span>
            <input class="input" name="region" value="<%= row.region || '' %>">
          </label>

          <label class="field full">
            <span>Vertical</span>
            <input class="input" name="vertical" value="<%= row.vertical || '' %>">
          </label>

          <label class="field full">
            <span>Funding</span>
            <input class="input" name="funding" value="<%= row.funding || '' %>">
          </label>

          <label class="field full">
            <span>LinkedIn</span>
            <input class="input" name="linkedin" value="<%= row.linkedin || '' %>">
          </label>

          <label class="field full">
            <span>Latest News / Source URL</span>
            <input class="input" name="latest_news" value="<%= row.latest_news || '' %>">
          </label>

          
          <label class="field">
            <span>Lead Status</span>
            <select class="input" name="lead_status">
              <option value="new" <%= row.lead_status==='new'?'selected':'' %>>new</option>
              <option value="contacted" <%= row.lead_status==='contacted'?'selected':'' %>>contacted</option>
              <option value="qualified" <%= row.lead_status==='qualified'?'selected':'' %>>qualified</option>
              <option value="disqualified" <%= row.lead_status==='disqualified'?'selected':'' %>>disqualified</option>
            </select>
          </label>
          <label class="field">
            <span>Owner</span>
            <input class="input" name="owner" value="<%= row.owner || '' %>">
          </label>
          <label class="field full">
            <span>Tags (comma-separated)</span>
            <input class="input" name="tags" value="<%= row.tags || '' %>">
          </label>
          <label class="field">
            <span>Source Confidence (0-100)</span>
            <input class="input" name="source_confidence" type="number" min="0" max="100" value="<%= row.source_confidence || '' %>">
          </label>
          <label class="field">
            <span>Enrich Status</span>
            <select class="input" name="enrich_status">
              <option value="pending" <%= row.enrich_status==='pending'?'selected':'' %>>pending</option>
              <option value="running" <%= row.enrich_status==='running'?'selected':'' %>>running</option>
              <option value="done" <%= row.enrich_status==='done'?'selected':'' %>>done</option>
              <option value="failed" <%= row.enrich_status==='failed'?'selected':'' %>>failed</option>
            </select>
          </label>

          <label class="field full">
            <span>Source</span>
            <input class="input" name="source" value="<%= row.source || '' %>">
          </label>

          <label class="field">
            <span>TiDB Potential Score (0-100)</span>
            <input class="input" name="tidb_potential_score" type="number" min="0" max="100" value="<%= row.tidb_potential_score || '' %>">
          </label>

          <label class="field full">
            <span>TiDB Potential Reason</span>
            <textarea class="input" name="tidb_potential_reason"><%= row.tidb_potential_reason || '' %></textarea>
          </label>

          <label class="field full">
            <span>Manual Note</span>
            <textarea class="input" name="manual_note"><%= row.manual_note || '' %></textarea>
          </label>
        </div>

        <div style="margin-top:12px;display:flex;gap:10px;">
          <button class="btn primary" type="submit">保存并锁定</button>
          <a class="btn" href="/">取消</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
